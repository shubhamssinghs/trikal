"""First seed — placeholder for initial data."""

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession


async def apply(session: AsyncSession):
    # Placeholder: add initial records, default configurations, etc.
    # Example: await session.execute(text("INSERT INTO ... VALUES (...)"))
    pass
